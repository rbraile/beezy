import React from 'react'
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'

const Icon = (iconFa, size) => {
  return <FontAwesomeIcon icon={iconFa} size={size} />
}

export default Icon
